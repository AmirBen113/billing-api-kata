server:
  port: 8888
app:
  external-api:
    catalog-url: "http://localhost:8888/external-api"

resilience4j.circuitbreaker:
  instances:
    catalogService:
      slidingWindowSize: 5
      registerHealthIndicator: true

logging:
  level:
    fr.caassurances.kata: WARN